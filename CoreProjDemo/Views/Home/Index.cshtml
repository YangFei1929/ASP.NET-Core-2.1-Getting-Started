@*@using Microsoft.Extensions.Options
@using CoreProjDemo.Settings*@
@model IEnumerable<CoreProjDemo.Models.TestTable>
@*@inject IOptions<ConnectionOptions> options*@
@{
    //ViewData["Title"] = "Index";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

<p>
    <a asp-action="Add">添加</a>
    @await Component.InvokeAsync("PersonList",new { bigerNum=2})
    <hr color="red"/>@*有两种方式引入ViewComponent*@
    <vc:person-list biger-num="2"></vc:person-list>
</p>
<div class="container">
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Age)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Sex)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Remarks)
                    </th>
                    <th></th>
                    @*<th>@options.Value.SqlServerConnection</th>*@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Age)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Sex)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Remarks)
                        </td>
                        <td>
                            <a  asp-controller="Home" asp-action="Edit" asp-route-Id="@item.Id">编辑</a>

                            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
